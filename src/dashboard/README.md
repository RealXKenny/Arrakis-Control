# Dashboard structure

- `index.html` — public React landing page.
- `styles.css` — shared visual theme.
- `server.js` — OAuth, protected APIs, owner controls, and portal rendering.

Keep presentation in the frontend files and authentication/API logic in the server.
