# Dashboard backend

HTTP routes, OAuth session handling, Discord authorization, and Dune Console API access belong here.

The compatibility entrypoint is `server.js` in this folder; it delegates to the existing dashboard server while the route modules are split out incrementally.
