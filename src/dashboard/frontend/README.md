# Dashboard frontend

React/Vite source, HTML entrypoints, CSS, images, map artwork, and other browser assets belong in this folder.

- `index.html` is the Vite entrypoint.
- `src/` contains React components and browser logic.
- `public/` contains static assets such as map images and icons.
